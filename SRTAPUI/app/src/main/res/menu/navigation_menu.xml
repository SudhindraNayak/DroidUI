<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">
    <group android:checkableBehavior="single">
        <item android:title="Add Fence"
            android:id="@+id/fence_id"
            android:icon="@drawable/ic_add_trip" />
        <item android:title="Notifications"
            android:id="@+id/notify_id"
            android:icon="@drawable/ic_notification" />
        <item android:title="Add Trips"
            android:id="@+id/add_trip_id"
            android:icon="@drawable/ic_add_trip" />
        <item android:title="Add Route"
            android:id="@+id/add_route_id"
            android:icon="@drawable/ic_add_route" />
        <item android:title="View Trips"
            android:id="@+id/view_trip_id"
            android:icon="@drawable/ic_view" />
        <item android:title="Reassign Trips"
            android:id="@+id/re_trip_id"
            android:icon="@drawable/ic_reassign" />
    </group>
    <item android:title="Other options">
        <menu>
            <item android:title="Logout"
                android:id="@+id/logout_id"
                android:icon="@drawable/ic_logout" />
        </menu>
    </item>
</menu>